services:
  api:
    image: ghcr.io/kai/fake-book-api:latest

    container_name: fake_book_api

    restart: unless-stopped

    env_file:
      - .env

    ports:
      - '3000:3000'

    networks:
      - fake_book_network

networks:
  fake_book_network:
    external: true
